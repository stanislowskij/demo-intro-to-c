// Here is our declaration of the swap(), make_array(), and print_array() functions.
// When we include this header file, the pre-processor will
// look for their definitions in stuff.c

void swap(int *array, int array_length, int idx1, int idx2);
int *make_array(int array_length);
void print_array(int *array, int array_length);